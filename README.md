1. User inputs a question in **natural language**
2. LangChain:
   - Understands the intent
   - Converts it into a valid SQL query
3. SQL database executes the query
4. Results are returned in **human-readable format**
